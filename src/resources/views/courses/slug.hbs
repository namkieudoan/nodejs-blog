<div class="mt-4">
    <div class="row">
        <div class="col-lg-3 col-sm-12">
            <a href='#' class='btn btn-primary'>Xem khóa học</a>
            <p>Trình độ khóa học: {{course.level}}</p>
        </div>
        <div class="col-lg-9 col-sm-12">
            <h2>{{course.name}}</h2>
            <iframe width="930" height="523" src="https://www.youtube.com/embed/{{course.videoId}}" title="Course detail page" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p class="desc">{{course.description}}</p>
        </div>
    </div>
</div>